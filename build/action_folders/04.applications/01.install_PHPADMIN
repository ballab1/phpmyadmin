#!/bin/bash


tar xzf "${PHPADMIN['file']}" -C "/tmp"

mkdir -p "${WWW}"
mv "/tmp/phpMyAdmin-${PHPADMIN['version']}-all-languages/"* "${WWW}"
sed -i "s@define('CONFIG_DIR'.*@define('CONFIG_DIR', '/etc/phpmyadmin/');@" "${WWW}/libraries/vendor_config.php"
